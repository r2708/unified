# Core pipeline dependencies (see pyproject.toml for the canonical list)
huggingface_hub>=0.24.0
pyarrow>=15.0.0
numpy>=1.24
datasketch>=2.0,<3
xxhash>=3.4.1
boto3>=1.34
requests>=2.31
certifi>=2024.2.2
PyYAML>=6.0
orjson>=3.9
tqdm>=4.66
zstandard>=0.22

# Optional: exact GPT-style token counts instead of the byte heuristic
# tiktoken>=0.7

# Dev / tests
pytest>=8.0
